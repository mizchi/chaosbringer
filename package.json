{
  "name": "chaosbringer-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Workspace root for chaosbringer (and future Layer-1 packages extracted from it).",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "lint:nul": "pnpm -r lint:nul"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "duckdb",
      "esbuild"
    ]
  }
}
